DROP TABLE IF EXISTS ewm_hits;

CREATE TABLE IF NOT EXISTS ewm_hits (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    app VARCHAR(255),
    uri VARCHAR(255),
    ip VARCHAR(255),
    hit_time TIMESTAMP WITHOUT TIME ZONE
    );